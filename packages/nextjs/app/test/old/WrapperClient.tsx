// "use client";

// import dynamic from "next/dynamic";
// import type { FC } from "react";

// const Wrapper = dynamic(() => import("./Wrapper"), { ssr: false });
// // const Wrapper = import("./Wrapper");

// interface Props {
//   address: string;
// }

// const WrapperClient: FC<Props> = ({ address }) => {
//   return <Wrapper address={address} />;
// };

// export default WrapperClient;